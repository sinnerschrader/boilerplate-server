#!/usr/bin/env node
var path = require('path');
var shell = require('shelljs');

var executable = path.resolve(__dirname, '..', 'node_modules', '.bin', 'babel-node');
var target = path.resolve(__dirname, '_build');

shell.exec(executable + ' --experimental ' + target + ' ' + process.argv.slice(2).join(' '));
